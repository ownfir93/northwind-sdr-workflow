# Required. Postgres connection string used by Prisma.
DATABASE_URL=""

# Optional. If unset, the app uses deterministic fallback generation.
ANTHROPIC_API_KEY=""

# Optional model overrides. Leave blank unless the default aliases need to change.
ANTHROPIC_CONTEXT_MODEL=""
ANTHROPIC_DRAFT_MODEL=""
